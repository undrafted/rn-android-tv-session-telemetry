## Architecture

![Architecture](../.github/architecture.svg)

_(dashed = planned, not yet built. Generated from `.github/architecture.mmd` via
`npx @mermaid-js/mermaid-cli` — edit the `.mmd` source and regenerate, not the SVG.)_
