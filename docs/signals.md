### Telemetry Signals

![Signals](../diagrams/signals.svg)

_Generated from `diagrams/signals.mmd` via `npx @mermaid-js/mermaid-cli` — edit the `.mmd` source
and regenerate, not the SVG._

For what each signal's threshold/comparison actually is, which detector consumes it, and whether
it's wired into the fixture app, see [measurement-semantics.md](measurement-semantics.md).
