### Telemetry Signals

![Signals](../diagrams/signals.svg)

_Generated from `diagrams/signals.mmd` via `npx @mermaid-js/mermaid-cli` — edit the `.mmd` source
and regenerate, not the SVG._

Each signal's exact capture mechanism (and confirmation it's wired into the fixture app) is in
[Signal capture](measurement-semantics.md#signal-capture); which detector consumes it and at
what threshold is in [Analysis](measurement-semantics.md#analysis) — both in
measurement-semantics.md.
